---
import Availability from "./Availability.astro";

const links = [
  { href: "/#projects", label: "PROJECTS" },
  { href: "/about", label: "ABOUT" },
  { href: "/contact", label: "CONTACT" },
  // Browsers that opened the resume under the old 24-hour cache rule keep
  // serving their stale copy from that URL. The version marker gives them a
  // key they have never seen, so the click always fetches the current file.
  { href: "/dhruv-belekar-resume.pdf?v=2", label: "RESUME", external: true },
];
---

{/*
  The header sat on `backdrop-blur-sm`, which made the browser re-blur the
  full width of whatever was passing behind it on every scrolled frame. Over a
  background that is already 90% opaque white the blur had almost nothing left
  to show, so it cost a repaint a frame to be invisible. Put `backdrop-blur-sm`
  back if the difference turns out to be visible after all.
*/}
<header class="sticky top-0 z-50 bg-white/90">
  <nav class="mx-auto grid max-w-[1400px] grid-cols-[1fr_auto_1fr] items-center gap-2 px-4 py-5 sm:gap-4 sm:px-6 md:px-10">
    <a href="/" aria-label="Home" class="shrink-0 justify-self-start text-blue">
      <img
        src="/icons/logo-mark.webp"
        alt=""
        class="h-14 w-14 rounded-lg transition-transform duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] hover:-rotate-6 hover:scale-105 active:scale-95 sm:h-18 sm:w-18"
      />
    </a>
    <ul class="flex flex-nowrap items-center justify-self-center gap-x-2 whitespace-nowrap text-[10px] font-bold tracking-wide text-blue sm:gap-x-6 sm:text-sm">
      {
        links.map((link) => (
          <li>
            <a
              href={link.href}
              class="group relative block py-3 transition-colors duration-300 hover:text-ink"
              target={link.external ? "_blank" : undefined}
              rel={link.external ? "noreferrer" : undefined}
            >
              {link.label}
              <span
                aria-hidden="true"
                class="absolute inset-x-0 bottom-1.5 h-[2px] origin-left scale-x-0 rounded-full bg-current transition-transform duration-300 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:scale-x-100"
              />
            </a>
          </li>
        ))
      }
    </ul>
    <div class="hidden justify-self-end sm:block">
      <Availability />
    </div>
  </nav>
</header>
