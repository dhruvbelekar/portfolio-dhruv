---
import Layout from "../layouts/Layout.astro";
import IntroLoader from "../components/IntroLoader.astro";
import ScrollReveal from "../components/ScrollReveal";
import Marquee from "../components/Marquee";
import ProjectRow from "../components/ProjectRow.astro";
import ProjectFilterNav from "../components/ProjectFilterNav";
import BioCopy from "../components/BioCopy.astro";
import ExperienceSection from "../components/ExperienceSection.astro";
import {
  projects,
  filterCategories,
  testimonials,
  skillsRowA,
  skillsRowB,
  clientLogos,
} from "../data/projects";
---

<Layout>
  <IntroLoader />

  <!-- Hero -->
  <section class="flex min-h-[calc(100svh-96px)] flex-col overflow-x-hidden px-6 sm:min-h-[calc(100svh-112px)]">
    <div class="mx-auto flex w-full max-w-[1400px] flex-1 flex-col justify-center pt-6">
      {/*
        The mark sits above the wordmark, off its axis so it reads as a sticker
        rather than a logo lockup. Both faces are stacked and cross-faded, which
        keeps the swap instant — a second file fetched on first hover would
        arrive after the pointer had gone. `active` carries it to touch, where
        there is no hover to trigger.
      */}
      <div
        class="group mx-auto w-[clamp(72px,var(--shell)*0.09,150px)] select-none"
        style="perspective: 600px;"
      >
        <div
          class="relative -rotate-6 transition-transform duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:rotate-3 group-hover:scale-105 group-active:rotate-3 group-active:scale-105 motion-reduce:transition-none"
        >
          <img
            src="/img/hero/face.webp"
            width={420}
            height={420}
            alt="Dhruv Belekar, drawn in blue line art"
            fetchpriority="high"
            class="w-full transition-opacity duration-300 ease-out group-hover:opacity-0 group-active:opacity-0"
          />
          <img
            src="/img/hero/face-tongue.webp"
            width={420}
            height={420}
            alt=""
            aria-hidden="true"
            class="absolute inset-0 w-full opacity-0 transition-opacity duration-300 ease-out group-hover:opacity-100 group-active:opacity-100"
          />
        </div>
      </div>

      <h1
        class="mt-[clamp(0.75rem,var(--shell)*0.02,28px)] select-none text-center font-bold leading-[0.8] text-blue"
        style="font-family: var(--font-display); font-size: clamp(1.5rem, var(--shell) * 0.135, 380px); letter-spacing: -0.05em;"
      >
        <span class="block whitespace-nowrap">dhruv belekar</span>
      </h1>
      <p
        class="font-bold leading-tight text-gray-400"
        style="font-family: var(--font-display); font-size: clamp(1.125rem, var(--shell) * 0.028, 40px); text-indent: 42%; margin-top: clamp(1rem, var(--shell) * 0.048, 70px);"
      >
        I design <span class="text-blue">digital experiences</span>,{" "}
        create <span class="text-blue">visual stories</span>,{" "}
        and bring ideas to life through <span class="text-blue">design and film</span>.{" "}
        I also have a slight obsession with the color <span class="text-blue">blue</span>. You'll notice.
      </p>
    </div>

    <div class="-mx-6 mb-6 border-y border-black/5 py-4">
      <Marquee client:load speed={100} gap="4rem">
        {
          Array(6)
            .fill(clientLogos)
            .flat()
            .map((logo, i) => (
              <img
                key={i}
                src={logo.src}
                width={logo.width}
                height={logo.height}
                alt=""
                class="h-8 w-auto object-contain transition-transform duration-500 ease-out hover:scale-110 sm:h-10"
              />
            ))
        }
      </Marquee>
    </div>
  </section>

  <!-- Projects -->
  <section class="scroll-mt-24 px-6 pt-[var(--space-section)] md:px-10" data-projects-section>
    <div class="mx-auto max-w-[1400px]">
      <ScrollReveal client:load>
        <h2
          class="select-none text-center font-black leading-[0.9] text-blue"
          style="font-family: var(--font-display); font-size: var(--type-section);"
        >
          Projects
        </h2>
      </ScrollReveal>

      <div class="mt-12 pb-24">
        {
          projects.map((p, i) => (
            <div data-project-category={p.filterCategory}>
              <ScrollReveal client:load delay={0.05} y={40}>
                <ProjectRow
                  slug={p.slug}
                  title={p.title}
                  category={p.category}
                  image={p.thumb}
                  alt={p.thumbAlt}
                  description={p.description}
                  timeline={p.timeline}
                  index={i}
                />
              </ScrollReveal>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <ProjectFilterNav
    client:load
    categories={filterCategories}
    itemSelector="[data-project-category]"
    sectionSelector="[data-projects-section]"
  />

  <!-- About preview -->
  <section class="px-6 pt-[var(--space-section)] md:px-10">
    <div class="mx-auto max-w-[1400px]">
      <ScrollReveal client:load>
        <h2
          class="select-none text-center font-black leading-[0.9] text-blue"
          style="font-family: var(--font-display); font-size: var(--type-section);"
        >
          About Dhruv
        </h2>
      </ScrollReveal>

      <div class="mt-12 grid grid-cols-1 items-start gap-6 sm:grid-cols-[280px_1fr] sm:gap-10 md:grid-cols-[360px_1fr]">
        <ScrollReveal client:load>
          <div class="group w-full focus:outline-none" tabindex="0">
            <div class="relative aspect-square w-full overflow-hidden rounded-2xl group-focus-visible:ring-2 group-focus-visible:ring-blue">
              <img
                src="/img/about/portrait-illustration.png"
                alt="Illustrated portrait of Dhruv"
                class="absolute inset-0 h-full w-full object-cover transition-opacity duration-150 ease-out group-hover:opacity-0 group-focus-within:opacity-0"
              />
              <img
                src="/img/about/portrait-illustration-tongue.webp"
                alt=""
                aria-hidden="true"
                class="absolute inset-0 h-full w-full object-cover opacity-0 transition-opacity duration-150 ease-out group-hover:opacity-100 group-focus-within:opacity-100"
              />
            </div>
            <p class="mt-3 text-center text-sm font-bold text-blue transition-opacity duration-200 ease-out group-hover:opacity-0 group-focus-within:opacity-0">
              <span class="sm:hidden">Tap me</span>
              <span class="hidden sm:inline">Hover over me</span>
            </p>
          </div>
        </ScrollReveal>

        <ScrollReveal client:load delay={0.1} className="min-w-0">
          <BioCopy />
        </ScrollReveal>
      </div>

    </div>
  </section>

  <!-- Skills marquee (full-bleed, edge to edge) -->
  <div class="mt-10 space-y-2 py-2 text-2xl font-bold tracking-[-0.05em] text-blue sm:text-3xl">
    <Marquee client:load speed={90}>
      {
        Array(8)
          .fill(skillsRowA)
          .flat()
          .map((s, i) => (
            <span key={i} class="whitespace-nowrap">
              {s} <span class="text-gray-300">✦</span>
            </span>
          ))
      }
    </Marquee>
    <Marquee client:load speed={105} direction="right">
      {
        Array(8)
          .fill(skillsRowB)
          .flat()
          .map((s, i) => (
            <span key={i} class="whitespace-nowrap">
              {s} <span class="text-gray-300">✦</span>
            </span>
          ))
      }
    </Marquee>
  </div>

  <section class="px-6 md:px-10">
    <div class="mx-auto max-w-[1400px]">
      <ScrollReveal client:load className="mt-12">
        <ExperienceSection />
      </ScrollReveal>
    </div>
  </section>

  <!-- Testimonials heading -->
  <section class="px-6 pt-16 md:px-10">
    <div class="mx-auto max-w-[1400px]">
      <ScrollReveal client:load>
        <h2
          class="select-none text-center font-black leading-[0.9] text-blue"
          style="font-family: var(--font-display); font-size: var(--type-section);"
        >
          Testimonials
        </h2>
      </ScrollReveal>
    </div>
  </section>

  <!-- Testimonial cards -->
  <section class="px-6 pb-16 pt-10 md:px-10">
    <div class="mx-auto max-w-[1400px]">
      <div class="flex flex-col gap-2">
        <Marquee client:load speed={65} gap="1rem">
          {
            Array(4)
              .fill([testimonials[2], testimonials[3]])
              .flat()
              .map((t, i) => (
                <div
                  key={i}
                  class="group flex h-[185px] w-[320px] shrink-0 flex-col rounded-2xl p-6 transition-[transform,box-shadow,background-color] duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] hover:-translate-y-1.5 hover:bg-white hover:shadow-[0_18px_40px_rgba(19,67,240,0.10)]"
                >
                  <div class="flex items-center gap-3">
                    <img
                      src={t.avatar}
                      alt=""
                      class="h-10 w-10 shrink-0 rounded-full object-cover transition-transform duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:scale-110"
                    />
                    <div>
                      <p class="font-bold text-ink transition-colors duration-300 group-hover:text-blue">
                        {t.name}
                      </p>
                      <p class="text-sm text-ink-muted">{t.role}</p>
                    </div>
                  </div>
                  <p class="mt-4 line-clamp-4 text-sm text-ink-muted">{t.quote}</p>
                </div>
              ))
          }
        </Marquee>
        <Marquee client:load speed={72} gap="1rem" direction="right">
          {
            Array(4)
              .fill([testimonials[0], testimonials[1]])
              .flat()
              .map((t, i) => (
                <div
                  key={i}
                  class="group flex h-[185px] w-[320px] shrink-0 flex-col rounded-2xl p-6 transition-[transform,box-shadow,background-color] duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] hover:-translate-y-1.5 hover:bg-white hover:shadow-[0_18px_40px_rgba(19,67,240,0.10)]"
                >
                  <div class="flex items-center gap-3">
                    <img
                      src={t.avatar}
                      alt=""
                      class="h-10 w-10 shrink-0 rounded-full object-cover transition-transform duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:scale-110"
                    />
                    <div>
                      <p class="font-bold text-ink transition-colors duration-300 group-hover:text-blue">
                        {t.name}
                      </p>
                      <p class="text-sm text-ink-muted">{t.role}</p>
                    </div>
                  </div>
                  <p class="mt-4 line-clamp-4 text-sm text-ink-muted">{t.quote}</p>
                </div>
              ))
          }
        </Marquee>
      </div>
    </div>
  </section>
</Layout>
